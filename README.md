# Handwritten Digit Recognition Using MNIST
(A work in progress). The first simple AI model I built using MNIST dataset, to recognize handwritten digits.

## Improvements (Further Goals)
I want to extend this model to recognize handwritten letters and characters from other languages!